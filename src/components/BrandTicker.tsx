import { useTranslation } from 'react-i18next'
export function BrandTicker() { const {t}=useTranslation(); const brands=t('ticker.brands',{returnObjects:true}) as string[]; const items=[...brands,...brands]; return <section aria-label="Television brands we service" className="relative flex h-[100px] sm:h-[112px] items-center overflow-hidden py-5 sm:py-6"><div className="pointer-events-none absolute inset-x-0 top-1/2 h-16 -translate-y-1/2 bg-white/25 blur-2xl"/><div className="brand-ticker relative w-full"><div className="brand-ticker-track">{items.map((brand,index)=><span key={`${brand}-${index}`} className="inline-flex items-center text-[22px] sm:text-[26px] font-medium uppercase tracking-[0.12em] text-[#12281A]/65 whitespace-nowrap"><span>{brand}</span><span className="brand-ticker-separator mx-7 sm:mx-10 w-1.5 h-1.5 shrink-0 rounded-full bg-[#42A85D]"/></span>)}</div></div></section> }
